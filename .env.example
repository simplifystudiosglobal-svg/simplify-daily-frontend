# VITE_API_BASE_URL: the deployed backend's URL (no trailing slash), e.g.
# https://simplify-daily-api.onrender.com
# Leave unset for local dev — vite.config.ts proxies /api and /rss.xml to a locally
# running backend at http://localhost:3001 instead.
VITE_API_BASE_URL=""
